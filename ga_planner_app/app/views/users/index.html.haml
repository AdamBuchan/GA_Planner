= link_to('Create New User', new_user_path)

%ul
  - @users.each do |user|
    %li
      %h4= link_to("#{user.first_name} #{user.last_name}", user_path(user))
      %p= user.level
      %p= image_tag(user.image)